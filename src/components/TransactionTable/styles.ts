import styled from "styled-components";

export const Container = styled.header`
  margin-top: 4rem;
  
  table {
    border-spacing: 0 0.5rem;
    width: 100%;
    color: var(--text-body);

    th {
      padding: 1rem 2rem;
      text-align: left;
    }
  }
`;